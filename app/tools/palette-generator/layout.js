export const metadata = {
  title: 'Color Palette Generator | Theme and Color',
  description: 'Free online color palette generator. Create hex palettes for UI, web, and brand design. Brand color palette generator and website color palette tool.',
};

export default function PaletteGeneratorLayout({ children }) {
  return children;
}
